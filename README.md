- `python get-inv.py <ID>` to get invite link
- `python main.py <TAG>` tag to scrape
- `pip install curl_cffi beatifulsoup4 lxml loguru`

## READ
Scraper gets ratelimited after maybe 90 servers but it goes by recently bumped so who cares
You can't filter out verified servers unless you wanna spend shekels on capsolver + toookens
